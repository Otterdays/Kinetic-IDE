org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
# AGP JdkImageTransform needs a full JDK with bin/jlink. IDEs (e.g. Cursor + Red Hat Java) may run Gradle on a JRE
# that has no jlink — then :app:compileDebugJavaWithJavac fails. Point this at JDK 17+ (forward slashes OK on Windows).
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-25.0.2.10-hotspot
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
